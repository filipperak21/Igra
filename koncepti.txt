1. CRUD



savePlayer();     // Create
showPlayers();    // Read
updatePlayer();   // Update
deletePlayer();   // Delete
2. Primitivni tipovi podataka


int score;
char name[MAX_NAME];
3. Složeni tip podataka – struct



typedef struct Player
{
    char name[MAX_NAME];
    int score;

} Player;
4. typedef



typedef struct Player Player;
5. enum



typedef enum MenuOption
{
    MENU_PLAY = 1,
    MENU_SHOW,
    MENU_UPDATE,
    MENU_DELETE,
    MENU_EXIT

} MenuOption;
6. static



static const char* fileName = "data.txt";
7. extern



extern int playerCount;
8. inline funkcija



static inline int randomWord(void)
{
    return rand() % WORD_COUNT;
}
9. Izbornik (Menu)


switch ((MenuOption)choice)
{
case MENU_PLAY:
    startGame();
    break;
}
10. Organizacija izvornog koda


main.c
game.c
game.h
player.c
player.h